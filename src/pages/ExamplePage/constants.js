// This would contain constants used in the ExamplePage
// e.g. action types, error messages, etc
// This would also contain all data-testid attributes and fixed text e.g. headers, labels, button text etc
// This is so both the tests and the components share the single source of truth for these values
