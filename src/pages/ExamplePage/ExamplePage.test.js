// This contains the tests for the ExamplePage
// It would contain the end to end tests on a simulated rendering of the page
// It would assert that the page behaves as expected
// e.g. if the page is rendered, it calls a certain function
// e.g. if the user clicks a button, the correct API call is made
// e.g. if the user submits a form, the correct data is sent to the server
